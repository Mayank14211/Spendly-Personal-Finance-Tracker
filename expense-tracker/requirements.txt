# Core Flask Framework
Flask==3.1.3
Werkzeug==3.1.6
Jinja2==3.1.2

# Security & Hashing
# werkzeug.security provides password hashing utilities (included in werkzeug)

# Testing
pytest==8.3.5
pytest-flask==1.3.0

# Development & Utilities (optional but recommended)
python-dotenv==1.0.1

# Database (sqlite3 is built-in to Python, no package needed)
